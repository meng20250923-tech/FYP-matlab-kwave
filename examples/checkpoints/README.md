# Example checkpoint

`fno_only_periodic_large.pt` is the trained FNO-only checkpoint used for the
principal large periodic benchmark. It records the model state, architecture,
normalisation statistics, physical setting, and training arguments required for
CPU inference on the fixed sample batch.
